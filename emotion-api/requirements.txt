# FastAPI Backend Requirements for Wav2Vec2 Emotion Detection
# Install with: pip install -r requirements.txt

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Machine Learning - Hugging Face Transformers
# Use newer versions with pre-built wheels to avoid Rust compilation
transformers>=4.40.0  # Newer version with pre-built tokenizers
torch>=2.6.0  # Compatible with available versions (2.6.0, 2.7.0, 2.7.1, 2.8.0, 2.9.0)
torchaudio>=2.6.0  # Compatible torchaudio version
tokenizers>=0.19.0  # Pre-built wheels available (install before transformers)

# Audio Processing
soundfile>=0.12.1  # Use >= for flexibility
pydub>=0.25.1  # Use >= for flexibility
librosa>=0.10.1  # Use >= for flexibility (has many dependencies)

# Data Processing
numpy>=1.24.0  # Compatible numpy version
scipy>=1.10.0  # Signal processing for filters

# Utilities
python-dotenv==1.0.0
protobuf>=3.20.0  # Required for transformers/Hugging Face

# Noise/VAD
webrtcvad>=2.0.10
noisereduce>=3.0.0

